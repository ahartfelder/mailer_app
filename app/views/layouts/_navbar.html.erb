<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Mailer App', root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><%= link_to 'Users', users_path, class:"nav-link #{"active" if request.path == users_path}" %></li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle <%= "active" if request.path.match(/post/) %>" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Posts 
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><%= link_to 'New Post', new_post_path, class:"dropdown-item" if logged_in? %></li>
            <li><%= link_to 'View Posts', posts_path, class:"dropdown-item" %></li>
          </ul>
        </li>
        <% if logged_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle <%= "active" if request.path.match(/user_path/) %>" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= current_user.f_name %>
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><%= link_to 'Edit Profile', [:edit, current_user], class:"dropdown-item" %></li>
            <li><hr class="dropdown-divider"></li>
            <li><%= link_to 'Delete User', new_user_path, class:"dropdown-item text-danger", method: :delete, data: { confirm: "Are you sure?" } %></li>
          </ul>
        </li>
        <li class="nav-item"><%= button_to 'Logout', logout_path, class:"nav-link btn btn-danger text-light me-1", method: :delete %></li>
        <% else %>
        <li class="nav-item"><%= link_to 'Login', login_path, class:"nav-link btn btn-primary text-light me-1" %></li>
        <li class="nav-item"><%= link_to 'Sign Up', new_user_path, class:"nav-link btn btn-success text-light me-1" %></li>
        <% end %>
      </ul>
      <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>
